import { Theme } from "react-native-elements";

const theme = {
  colors: {
    secondary: '#073B4C',
    primary: '#00bcd4',
    error: '#EF476F',
    warning: '#FFD166',
    success: '#06D6A0',
    grey5: '#f0f7fc'
  }
} as Theme;

export default theme;
